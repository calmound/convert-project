## 一、文件 / 文档格式转换（1–20）

1. PDF → Word
2. Word → PDF
3. Excel → CSV
4. CSV → Excel
5. PPT → PDF
6. PDF → PPT
7. TXT → PDF
8. Markdown → PDF
9. Markdown → HTML
10. HTML → PDF
11. HTML → Markdown
12. EPUB → PDF
13. MOBI → EPUB
14. 图片 → PDF
15. PDF → 图片（PNG/JPG）
16. Word → Markdown
17. LaTeX → PDF
18. PDF → LaTeX
19. JSON → CSV
20. CSV → JSON

---

## 二、图片 / 多媒体转换（21–40）

21. JPG → PNG
22. PNG → JPG
23. WebP → JPG
24. SVG → PNG
25. HEIC → JPG
26. GIF → MP4
27. MP4 → GIF
28. 视频 → 音频（MP4 → MP3）
29. WAV → MP3
30. MP3 → WAV
31. 视频分辨率转换（4K → 1080P）
32. 视频帧率转换
33. 图片尺寸批量转换
34. 图片颜色模式转换（RGB ↔ CMYK）
35. 图片灰度化
36. 图片 DPI 转换
37. 音频采样率转换
38. 音频比特率转换
39. 视频编码格式转换（H264 ↔ H265）
40. 视频容器转换（MKV ↔ MP4）

---

## 三、数据 / 编码 / 开发者工具（41–60）

41. JSON → YAML
42. YAML → JSON
43. XML → JSON
44. JSON → XML
45. Base64 编码 → 解码
46. URL 编码 → 解码
47. Unicode → 中文
48. 中文 → Unicode
49. 时间戳 → 日期时间
50. 日期时间 → 时间戳
51. Hex → RGB
52. RGB → Hex
53. 大小写转换（upper/lower）
54. 驼峰命名 → 下划线命名
55. 下划线命名 → 驼峰命名
56. SQL → ORM Model
57. ORM Model → SQL
58. OpenAPI → 接口文档
59. CSV → SQL Insert
60. SQL 查询结果 → CSV

---

## 四、文本 / 内容转换（61–75）

61. 简体中文 → 繁体中文
62. 繁体中文 → 简体中文
63. 英文 → 中文
64. 中文 → 英文
65. 文本去重
66. 文本去空行
67. 文本格式化（对齐/缩进）
68. Markdown → 富文本
69. 富文本 → Markdown
70. 文本 → HTML
71. HTML → 纯文本
72. 文本编码格式转换（UTF-8 ↔ GBK）
73. 正则匹配结果 → 列表
74. 文本 → 关键词列表
75. 文本 → 词频统计

---

## 五、单位 / 数值 / 工程换算（76–90）

76. 长度单位转换（m ↔ km ↔ inch）
77. 面积单位转换
78. 体积单位转换
79. 重量单位转换
80. 温度单位转换（℃ ↔ ℉）
81. 速度单位转换
82. 压力单位转换
83. 能量单位转换
84. 电压 / 电流单位转换
85. 货币转换（实时汇率）
86. 文件大小单位转换（KB ↔ MB ↔ GB）
87. 进制转换（2/8/10/16）
88. 百分比 ↔ 小数
89. 科学计数法 ↔ 普通数字
90. 时间单位转换（秒 ↔ 分钟 ↔ 小时）

---

## 六、业务 / 实用工具转换（91–100）

91. 手机号 → 运营商识别
92. IP → 地理位置
93. User-Agent → 设备信息
94. 身份证号 → 基本信息解析
95. 银行卡号 → 发卡行识别
96. Emoji → Unicode
97. Unicode → Emoji
98. Cron 表达式 → 可读时间
99. 可读时间 → Cron 表达式
100. 表单数据 → JSON Schema

